[package]
name = "agent-bazaar"
version = "0.1.0"
description = "Agent Bazaar — registry + x402 escrow on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "agent_bazaar"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
